# Heavy-Light Decomposition

A generic Rust crate for heavy-light decomposition (HLD) that enables efficient path queries and updates on trees. Supports customizable combine functions, lazy propagation, and all standard HLD operations including LCA queries, path queries, and path updates with O(log² n) complexity.
